# Icon system

The original 24×24 SVG set uses rounded linear strokes restored from the first
T02 implementation. The settings gear follows the same line weight and corner
rules. Every icon uses `currentColor`; the React `Icon` component uses these
files as masks so theme colour remains inherited.
