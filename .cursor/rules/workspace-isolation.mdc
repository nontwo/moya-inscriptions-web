---
description: Restrict all work to the Moya project and its own registered worktrees
alwaysApply: true
---

# Moya Project Isolation

- Treat `/Users/pengxiangshuai/Developer/moya-inscriptions-web` and Git
  worktrees registered by that repository as the entire allowed project scope.
- Only inspect, edit, build, test, or run services belonging to this repository.
- Never read from, copy from, write to, or import code, configuration,
  dependencies, environment files, generated assets, data, or implementation
  ideas from another local project.
- Never stop, reconfigure, reuse, or otherwise affect another project's
  processes, ports, worktrees, Git state, dependencies, or caches.
- Keep task implementation in the dedicated Moya worktree and preserve all
  pre-existing user changes in the primary checkout.
- Temporary diagnostics may use OS temporary storage, but they must contain
  only artifacts produced from this Moya repository and must never be treated
  as another source project.
