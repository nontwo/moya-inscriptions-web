# Historical candidate placeholder; do not use as current configuration.
# 变量名称与仓库根目录 .env.example 保持一致。
NODE_ENV=development
DATABASE_URL="<inject-from-secret-management>"
OBJECT_STORAGE_BUCKET="<development-object-storage-bucket>"
OBJECT_STORAGE_REGION="<mainland-region>"
PUBLIC_CDN_BASE_URL="https://assets.example.invalid"
MAP_PROVIDER_KEY="<optional-restricted-development-key>"
