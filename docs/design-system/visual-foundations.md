# 视觉基础

## 颜色

浅色页面以 `#F4F0E6` 为基底，比参考古纸的中位色 `#DACFB3`
更白、更克制；深色页面使用 `#24231F` 灰黑纸张，不使用纯黑。正文分别使用
`#27241F` 与 `#E7E1D6`，印泥强调分别使用 `#B34A32` 与 `#CE6F54`。

全部组件色彩必须经 `--yoyi-color-*` 语义变量使用。`text-primary`、
`text-secondary`、`text-tertiary`
和印泥色在对应页面背景上满足普通正文对比要求；disabled 状态不承载必要信息。

## 排版

普通文字使用系统字体栈：Apple 系统字体、PingFang SC、Hiragino Sans GB、Microsoft
YaHei、system-ui、sans-serif。公共组件的移动/桌面字号由 CSS
variable 在 896px 切换；正文保持 16px 与 1.75 行高。

底部导航的“首页、碑刻、书帖”直接使用用户提供图片裁切得到的三个透明 PNG；顶部“发现、附近”、动态标题、检索结果和用户内容均使用系统字体真实文本。

## 纸张纹理

四种纹理均使用 384×512 透明平铺 SVG：纵向纤维、低频色差和细颗粒分别由独立
`feTurbulence`
层生成。手机主界面使用 visible 版本，其他长期阅读面可使用 subtle 版本；透明度不高于 6.8%，不得叠加折痕、烧边或位图。

## 响应式

- 360/390px：双列图片流、横向滚动标签、固定三项底栏；手机主预览画布最大宽度430px。
- 768px：平板全宽布局，继续采用触控向主导航（竖屏底栏 / 横屏侧轨），增加内容留白与卡片宽度。
- 896px：公共 UI 组件保持 `navigation` 断点语义；正式页面可在此切换桌面导航。
- 1024px：`mobile-preview`
  原型特例在此从手机/平板视觉壳切换到归档语言的全屏桌面壳，以便同一链接审核两套方案。
- 1440px：桌面壳保持 1200px 内容上限；详情阅读宽度不超过 760px。

该 1024px 壳切换只属于非生产交互原型，不改变 `navigation=896px`
的公共 token、组件 API 或正式页面约定。组件目录中的手机画布演示仍可按需限制宽度，正式接入页面应遵循公共断点。
