# ArtVenn Apple app

The existing Xcode Multiplatform project is in `ArtVenn/ArtVenn.xcodeproj`.
Its sibling source, resource, and test folders retain the default Xcode layout.

From this task worktree root, open it with:

```sh
open apps/apple/ArtVenn/ArtVenn.xcodeproj
```

Personal Xcode state stays local and is excluded from Git.
